# Secrets — NEVER commit these
.env
*.env
!.env.example

# MediaWiki generated config (holds DB password + secret keys)
/mediawiki/LocalSettings.php

# Third-party skins (cloned via scripts/install-skins.sh)
/skins/

# Database dumps / backups
*.sql
*.dump
backups/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
