# Pretty MediaWiki URLs: /wiki/Page_Name is served by index.php via PATH_INFO.
# Loaded into Apache at /etc/apache2/conf-enabled/shorturl.conf.
# Paired with $wgArticlePath = '/wiki/$1' and $wgUsePathInfo = true.
Alias /wiki /var/www/html/index.php
AcceptPathInfo On
